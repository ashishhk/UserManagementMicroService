import React, {Component} from 'react';

class SearchPortal extends Component{
    render(){
        return(
            <div>SEARCH Portal (Default)</div>
        );
    }
}

export default SearchPortal;